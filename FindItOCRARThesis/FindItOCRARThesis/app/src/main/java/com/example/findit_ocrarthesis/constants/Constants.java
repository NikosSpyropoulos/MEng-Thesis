package com.example.findit_ocrarthesis.constants;
import android.os.Environment;

import java.io.File;

public class Constants {

    // constants for Image to speech
    public static final String DEFAULT_LANGUAGE = "ell";
    public static final String DATAPATH = Environment.getExternalStorageDirectory().getAbsolutePath() + File.separator;
}